import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Osudake Katakana Hiragana',
  webDir: 'www'
};

export default config;
